#
# This workflow calls the main distribution pipeline from DuckDB to build, test and (optionally) release the extension
#
name: Stable Extension Distribution Pipeline
on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - '**/README.md'
      - 'doc/**'
  push:
    branches:
      - main
    paths-ignore:
      - '**/README.md'
      - 'doc/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' || github.sha }}
  cancel-in-progress: true

jobs:
  duckdb-stable-build:
    name: Build extension binaries
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@16d89a59ee14904a62383e83c300432b19d29abc
    with:
      duckdb_version: 3a3967aa8190d0a2d1931d4ca4f5d920760030b4
      ci_tools_version: 16d89a59ee14904a62383e83c300432b19d29abc
      exclude_archs: "wasm_mvp;wasm_eh;wasm_threads;windows_amd64;windows_amd64_mingw"
      extension_name: substrait

  duckdb-stable-deploy:
    name: Deploy extension binaries
    needs: duckdb-stable-build
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_deploy.yml@16d89a59ee14904a62383e83c300432b19d29abc
    secrets: inherit
    with:
      duckdb_version: 3a3967aa8190d0a2d1931d4ca4f5d920760030b4
      ci_tools_version: 16d89a59ee14904a62383e83c300432b19d29abc
      exclude_archs: "wasm_mvp;wasm_eh;wasm_threads;windows_amd64;windows_amd64_mingw"
      extension_name: substrait
      deploy_latest: true
